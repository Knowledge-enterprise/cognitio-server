import { Response } from '../utils';

export default class Users {
  static getUser(req, res) {
    Response.unImplemented(res);
  }

  static getAllUsers(req, res) {
    Response.unImplemented(res)
  }
}